name: GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'
      - run: yarn
      - run: |
          git config --global user.email "mupsys@protonmail.com"
          git config --global user.name "mupsys"

      - run: yarn deploy
        env:
          PA_TOKEN: ${{ secrets.PA_TOKEN }}
          CI: true